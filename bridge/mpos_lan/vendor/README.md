# Vendor files are intentionally excluded

Place the licensed MPOS vendor DLL/header here only in a local or deployment package. Do not commit them.

Expected runtime path by default:

    bridge/mpos_lan/vendor/FDK_Module_64bit.dll

Confirm vendor license, version, architecture and checksum before deployment.
